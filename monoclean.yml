workspace:
  name: "todo"
  version: "1.0.0"

business-rules:
  "@todo/business-rules":
    folder: "business-rules"
    automations:
      "events/allSupportedEvents": "events/definitions"

executables:    
  "@todo/server":
    folder: "apps/server"

  "@todo/client":
    folder: "apps/client"

adapters:
  "@todo/fake-repository":
    folder: "adapters/fake-repository"
    
  "@todo/postgres-repository":
    folder: "adapters/postgres-repository"
